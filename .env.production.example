# =============================================================================
# AXIS Platform - Production Environment Variables
# =============================================================================
# These are the ONLY environment variables needed on the VPS.
# All other configurations are managed via the Admin Panel -> System Settings
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Connection (REQUIRED - Bootstrap Only)
# Get these from: https://supabase.com/dashboard/project/<project-id>/settings/api
# -----------------------------------------------------------------------------
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# -----------------------------------------------------------------------------
# Frontend Build-time Variables
# These are injected during Docker build
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
NEXT_PUBLIC_API_URL=http://your-vps-ip-or-domain

# -----------------------------------------------------------------------------
# NOTE: The following configurations have been moved to the database
# and are managed via Admin Panel -> System Settings:
# -----------------------------------------------------------------------------
# - GEMINI_API_KEY        -> system_settings.gemini_api_key
# - OPENAI_API_KEY        -> system_settings.openai_api_key
# - ANTHROPIC_API_KEY     -> system_settings.anthropic_api_key
# - APIFY_TOKEN           -> system_settings.apify_token
# - META_APP_ID           -> system_settings.meta_app_id
# - META_APP_SECRET       -> system_settings.meta_app_secret
# - WAHA_API_KEY          -> system_settings.waha_api_key
# - WAHA_DASHBOARD_USER   -> system_settings.waha_dashboard_username
# - WAHA_DASHBOARD_PASS   -> system_settings.waha_dashboard_password
# - LANGFUSE_PUBLIC_KEY   -> system_settings.langfuse_public_key
# - LANGFUSE_SECRET_KEY   -> system_settings.langfuse_secret_key
# - CORS_ORIGIN           -> system_settings.cors_origin
# - REDIS_URL             -> Hardcoded in docker-compose (redis://redis:6379)
# - WAHA_API_URL          -> Hardcoded in docker-compose (http://waha:3000)
# =============================================================================
