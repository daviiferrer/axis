const AgenticNode = require('../AgenticNode');

class ObjectionNode extends AgenticNode {
    constructor(dependencies) {
        super(dependencies);
    }

    // Inherits execute logic from AgenticNode
}

module.exports = ObjectionNode;
